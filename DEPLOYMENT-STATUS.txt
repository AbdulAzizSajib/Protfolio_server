Portfolio Server cPanel Deployment Status
Date: April 10, 2026
Domain: express.ousadbazar.com

What has already been done:
1. Express backend project is prepared for server deployment.
2. Production build output exists in dist/app/server.js.
3. cPanel Node.js app was created for:
   /home/ousadbazar/nodeapps/portfolio_api
4. GitHub repository was cloned into cPanel.
5. Environment variables were added in cPanel.
6. Build failure was traced to missing dev type packages on cPanel.
7. A cPanel-specific build script was added in package.json:
   cpanel:build
8. The .htaccess file was identified as needing cleanup for Node.js Passenger routing.

Important current fixes needed:
1. .htaccess should NOT contain the SPA rewrite block for this API subdomain.
2. PassengerStartupFile should be:
   dist/app/server.js
3. Do NOT use:
   dist/src/app/server.js
4. .env on the server should use production values, not development.

Recommended .htaccess content for the API subdomain:
--------------------------------------------------
# php -- BEGIN cPanel-generated handler, do not edit
# This domain inherits the “PHP” package.
# php -- END cPanel-generated handler, do not edit

# DO NOT REMOVE. CLOUDLINUX PASSENGER CONFIGURATION BEGIN
PassengerAppRoot "/home/ousadbazar/nodeapps/portfolio_api"
PassengerBaseURI "/"
PassengerNodejs "/home/ousadbazar/nodevenv/nodeapps/portfolio_api/22/bin/node"
PassengerAppType node
PassengerStartupFile dist/app/server.js
# DO NOT REMOVE. CLOUDLINUX PASSENGER CONFIGURATION END
--------------------------------------------------

Recommended production environment values:
1. NODE_ENV=production
2. BETTER_AUTH_URL=https://express.ousadbazar.com
3. FRONTEND_URL=https://portfolio20-krx5snhwq-sajib-abdulazizs-projects.vercel.app
4. EMAIL_SENDER_SMTP_PORT=587
5. ACCESS_TOKEN_SECRET and REFRESH_TOKEN_SECRET must be real random secrets, not placeholders.

Current likely cause of the 503/failed startup:
1. Wrong or incomplete cPanel startup configuration.
2. .htaccess still contains the old rewrite block.
3. Server-side build was failing because TypeScript types were missing during cPanel build.
4. The app may still be running with development env values.

Next exact steps:
1. Update .htaccess and keep only the Passenger node block.
2. Confirm Startup File is dist/app/server.js.
3. Change NODE_ENV to production.
4. Change BETTER_AUTH_URL to https://express.ousadbazar.com.
5. Replace placeholder JWT secrets with real secrets.
6. Run cpanel:build.
7. Run prisma:migrate.
8. Restart the app.
9. Open https://express.ousadbazar.com and test.

Useful note:
If it still fails after this, check stderr.log and read the last 20-30 lines.
